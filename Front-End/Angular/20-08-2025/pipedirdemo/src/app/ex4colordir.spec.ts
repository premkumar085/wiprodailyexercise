import { Ex4colordir } from './ex4colordir';

describe('Ex4colordir', () => {
  it('should create an instance', () => {
    const directive = new Ex4colordir();
    expect(directive).toBeTruthy();
  });
});
